[project]
name = "task-cli"
version = "0.1.0"
description = "A task manager CLI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
task-cli = "task_cli.__main__:main"
